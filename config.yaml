# Copyright 2024 Canonical Ltd.
# See LICENSE file for licensing details.

options:
  port:
    type: int
    default: 5000
    description: HTTP port
  backend-mode:
    type: string
    default: production
    description: Which mode out of `development` or `production` the backend server should use
  secure-cookies:
    type: boolean
    default: false
    description: Whether cookies should require HTTPS
  volume-viewer-image:
    type: string
    default: docker.io/charmedkubeflow/filebrowser:2.27.0-8f098ff-20260901041644
    description: Volume Viewer OCI Image (PVCViewer)
